[agents]
max_threads = 4
max_depth = 1

[agents."terra-worker"]
description = "Bounded implementation and verification worker for Nugget MVP sprint tasks."
config_file = "./agents/terra-worker.toml"
nickname_candidates = ["Terra"]
